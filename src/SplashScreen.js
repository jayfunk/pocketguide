import React from 'react-native';
const {
  View,
  Text,
} = React;

export default SplashScreen = React.createClass({
  render: function(){
    return (
      <View>
        <Text>Splash</Text>
      </View>
    );

  }
});